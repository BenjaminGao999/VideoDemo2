<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gaos.videodemo2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".other.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".other.TinyWindowPlayActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="可使用小窗口模式播放"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".other.RecyclerViewActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="在RecylerView视频列表中"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".other.ChangeClarityActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="在全屏时可以切换清晰度"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".other.UseInFragActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="在fragment中使用"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".other.ProcessHome1Activity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Activity中时处理播放时按下Home键"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".other.ProcessHome2Activity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Fragment中时处理播放时按下Home键"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>